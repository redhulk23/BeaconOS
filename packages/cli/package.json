{
  "name": "@beacon-os/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "beacon": "./dist/index.js"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src/",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@beacon-os/common": "workspace:*",
    "@beacon-os/sdk": "workspace:*",
    "commander": "^13.0.0",
    "inquirer": "^12.3.0",
    "chalk": "^5.4.0",
    "ora": "^8.1.0",
    "yaml": "^2.6.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "tsup": "^8.3.0",
    "vitest": "^2.1.0"
  }
}
